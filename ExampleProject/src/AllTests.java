import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Suite.class)
@SuiteClasses({ TestExampleClass.class })
public class AllTests {

	// That it done
	
	// Now lets run the suite
	
	
	// You can see all the tests passed 
	// With once class we have one test class
	// And that test class contains all the test cases for corresponding functions
	
	// Thus, you should not have lots of files
	
	// Happy Coding !!
	// Happy Testing !!
}
